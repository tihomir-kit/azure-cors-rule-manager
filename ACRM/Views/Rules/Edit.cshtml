@model ACRM.Models.CorsRuleModel

<h2>Edit CORS Rule</h2>

<div>
    @using (Html.BeginForm(new { id = Model.Id }))
    {
        @Html.Partial("_RulesForm", Model)

        <div>
            <input type="submit" value="Save rule">
        </div>
    }
</div>

@Html.ActionLink("<-- Back to CORS rules", "Index")
